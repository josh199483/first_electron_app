{
  "name": "firstelectronapp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . AlarmClock --out AlarmClock --overwrite --platform=win32 --arch=x64 --icon=clock.png --prune=true --squirrel-install --ignore=node_modules/electron-* --electron-version=2.0.5 --ignore=AlarmClock-win32-x64 --version-string.CompanyName=Josh --version-string.ProductName=AlarmClock",
    "pack": "grunt"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.5",
    "electron-packager": "^12.1.0",
    "grunt": "^1.0.3",
    "grunt-electron-installer": "^2.1.0"
  },
  "dependencies": {
    "eslint": "^5.2.0",
    "moment": "^2.22.2",
    "node-notifier": "^5.2.1"
  }
}
